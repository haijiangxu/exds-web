# 通用定时任务模块实施指南

## 📋 概述

本指南详细描述了如何在电力交易辅助决策系统中实施通用定时任务模块，包括开发、部署、配置和运维的各个环节。

## 🚀 实施路线图

### 阶段一：基础框架搭建（第1-2周）

#### 1.1 核心框架开发

**任务清单：**
- [ ] 创建任务基类 `BaseTask`
- [ ] 实现任务注册器 `TaskRegistry`
- [ ] 开发调度服务 `SchedulerService`
- [ ] 实现任务持久化 `TaskPersistence`

**关键文件：**
```
webapp/
├── tasks/
│   ├── __init__.py
│   └── base_task.py           # 任务基类
├── services/
│   ├── __init__.py
│   ├── task_registry.py        # 任务注册器
│   ├── scheduler_service.py    # 调度服务
│   └── task_persistence.py     # 任务持久化
```

**开发要点：**
- 使用 APScheduler 作为底层调度引擎
- MongoDB 存储任务状态和执行历史
- 实现任务的自动发现和注册机制

#### 1.2 数据库设计

**集合设计：**

```javascript
// scheduler_jobs - 任务调度信息（APScheduler自动管理）
{
  _id: ObjectId,
  id: String,           // 任务ID
  name: String,         // 任务名称
  trigger: Object,      // 触发器配置
  next_run_time: Date,  // 下次执行时间
  ...
}

// task_execution_logs - 任务执行日志
{
  _id: ObjectId,
  task_name: String,           // 任务名称
  execution_id: String,        // 执行ID
  status: String,             // 状态：started/completed/failed
  start_time: Date,           // 开始时间
  end_time: Date,             // 结束时间
  execution_time: Number,     // 执行时长（秒）
  result: Object,             // 执行结果
  error_info: Object,         // 错误信息（如果有）
  created_at: Date
}

// task_configs - 任务配置
{
  _id: ObjectId,
  task_name: String,           // 任务名称
  enabled: Boolean,           // 是否启用
  schedule_config: Object,    // 调度配置
  retry_config: Object,       // 重试配置
  alert_config: Object,       // 告警配置
  created_at: Date,
  updated_at: Date
}

// daily_reports - 日报记录
{
  _id: ObjectId,
  report_date: Date,           // 报表日期
  report_type: String,         // 报表类型：daily/weekly/monthly
  data: Object,               // 报表数据
  send_result: Object,        // 发送结果
  created_at: Date
}
```

**索引创建：**

```javascript
// task_execution_logs 索引
db.task_execution_logs.createIndex({ "task_name": 1, "created_at": -1 })
db.task_execution_logs.createIndex({ "execution_id": 1 })
db.task_execution_logs.createIndex({ "status": 1, "created_at": -1 })

// task_configs 索引
db.task_configs.createIndex({ "task_name": 1 }, { unique: true })
db.task_configs.createIndex({ "enabled": 1 })
```

### 阶段二：监控和告警系统（第1周）

#### 2.1 任务监控服务

**核心功能：**
- 实时监控任务执行状态
- 记录详细的执行日志
- 提供任务执行统计信息
- 支持任务执行的查询和分析

**关键文件：**
```
webapp/services/
├── task_monitor.py          # 任务监控服务
└── task_executor.py         # 任务执行器
```

#### 2.2 告警机制

**告警场景：**
- 任务执行失败
- 任务执行超时
- 任务执行时间异常
- 系统资源不足

**告警渠道：**
- 邮件通知
- 钉钉/企微机器人
- 短信通知（关键任务）
- Webhook 集成

### 阶段三：API接口开发（第1-2周）

#### 3.1 任务管理API

**接口设计：**

```python
# API路由设计
GET    /api/v1/scheduler/tasks                    # 获取任务列表
GET    /api/v1/scheduler/tasks/{task_name}        # 获取任务详情
POST   /api/v1/scheduler/tasks/{task_name}/execute # 手动执行任务
POST   /api/v1/scheduler/tasks/{task_name}/schedule # 配置任务调度
PUT    /api/v1/scheduler/tasks/{task_name}/enable   # 启用/禁用任务
DELETE /api/v1/scheduler/tasks/{task_name}          # 删除任务
GET    /api/v1/scheduler/tasks/{task_name}/history  # 获取执行历史
GET    /api/v1/scheduler/statistics               # 获取统计信息
GET    /api/v1/scheduler/logs                     # 获取执行日志
```

#### 3.2 前端管理界面

**页面功能：**
- 任务列表展示和搜索
- 任务详情查看
- 任务配置管理
- 执行历史查看
- 实时执行状态监控
- 告警信息展示

### 阶段四：业务任务迁移（第1-2周）

#### 4.1 现有任务分析

**识别现有定时任务：**
- 检查代码中的定时任务逻辑
- 梳理业务需求和执行规则
- 评估迁移的复杂度和风险

#### 4.2 业务任务实现

**客户相关任务：**
- ✅ 客户状态自动更新任务
- ✅ 合同到期提醒任务
- 🔄 客户数据同步任务（待实现）

**合同相关任务：**
- ✅ 合同自动过期处理任务
- 🔄 合同续期提醒任务（待实现）
- 🔄 合同统计报表任务（待实现）

**数据管理任务：**
- ✅ 数据清理任务
- ✅ 日报生成任务
- 🔄 数据备份任务（待实现）
- 🔄 数据归档任务（待实现）

### 阶段五：测试和部署（第1周）

#### 5.1 测试策略

**单元测试：**
- 任务基类功能测试
- 任务注册器测试
- 调度服务测试
- 监控服务测试

**集成测试：**
- 任务执行流程测试
- 数据库操作测试
- API接口测试
- 告警机制测试

**性能测试：**
- 大量任务并发执行测试
- 长时间运行稳定性测试
- 资源使用情况监控

#### 5.2 部署配置

**环境配置：**
```python
# config/scheduler_config.py
import os

# 调度器配置
SCHEDULER_CONFIG = {
    "timezone": "Asia/Shanghai",
    "jobstores": {
        "default": {
            "type": "mongodb",
            "database": "exds",
            "collection": "scheduler_jobs"
        }
    },
    "executors": {
        "default": {
            "type": "threadpool",
            "max_workers": int(os.getenv("SCHEDULER_MAX_WORKERS", "20"))
        }
    },
    "job_defaults": {
        "coalesce": True,
        "max_instances": 3,
        "misfire_grace_time": 300
    }
}

# 告警配置
ALERT_CONFIG = {
    "enabled": os.getenv("ALERT_ENABLED", "true").lower() == "true",
    "email": {
        "smtp_server": os.getenv("SMTP_SERVER", "smtp.example.com"),
        "smtp_port": int(os.getenv("SMTP_PORT", "587")),
        "username": os.getenv("SMTP_USERNAME", ""),
        "password": os.getenv("SMTP_PASSWORD", ""),
        "recipients": os.getenv("ALERT_RECIPIENTS", "").split(",")
    },
    "webhook": {
        "url": os.getenv("WEBHOOK_URL", ""),
        "timeout": int(os.getenv("WEBHOOK_TIMEOUT", "30"))
    }
}
```

**Docker 配置：**
```dockerfile
# Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install -r requirements.txt

# 复制应用代码
COPY . .

# 设置环境变量
ENV PYTHONPATH=/app
ENV SCHEDULER_MAX_WORKERS=10
ENV ALERT_ENABLED=true

# 暴露端口
EXPOSE 8005

# 启动命令
CMD ["uvicorn", "webapp.main:app", "--host", "0.0.0.0", "--port", "8005"]
```

## 🔧 开发指南

### 1. 环境准备

```bash
# 1. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate     # Windows

# 2. 安装依赖
pip install fastapi uvicorn pymongo apscheduler

# 3. 配置环境变量
export MONGODB_URL="mongodb://localhost:27017/exds"
export ALERT_ENABLED="true"
```

### 2. 开发流程

#### 创建新任务

```python
# 1. 创建任务文件
# webapp/tasks/my_task.py

from webapp.tasks.base_task import BaseTask, TaskPriority
from typing import Dict, Any

class MyCustomTask(BaseTask):
    @property
    def task_name(self) -> str:
        return "my_custom_task"

    @property
    def task_description(self) -> str:
        return "我的自定义任务"

    @property
    def task_category(self) -> str:
        return "custom"

    async def execute(self, **kwargs) -> Dict[str, Any]:
        # 实现任务逻辑
        return {
            "success": True,
            "message": "任务执行成功"
        }

# 2. 任务会自动被发现和注册
```

#### 测试任务

```python
# tests/test_tasks.py

import pytest
from webapp.tasks.my_task import MyCustomTask

@pytest.mark.asyncio
async def test_my_custom_task():
    task = MyCustomTask()

    result = await task.execute()

    assert result["success"] is True
    assert "message" in result
```

### 3. 调试指南

#### 日志配置

```python
# config/logging_config.py
import logging

LOGGING_CONFIG = {
    "version": 1,
    "disable_existing_loggers": False,
    "formatters": {
        "detailed": {
            "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
        }
    },
    "handlers": {
        "file": {
            "class": "logging.handlers.RotatingFileHandler",
            "filename": "logs/scheduler.log",
            "maxBytes": 10485760,  # 10MB
            "backupCount": 5,
            "formatter": "detailed"
        },
        "console": {
            "class": "logging.StreamHandler",
            "formatter": "detailed"
        }
    },
    "loggers": {
        "webapp.tasks": {
            "handlers": ["file", "console"],
            "level": "INFO",
            "propagate": False
        }
    }
}
```

#### 常见问题排查

**问题1：任务不执行**
- 检查任务是否正确注册
- 验证调度配置是否正确
- 查看调度器是否正常运行

**问题2：任务执行失败**
- 检查数据库连接
- 验证任务执行环境
- 查看错误日志详情

**问题3：任务执行超时**
- 检查任务逻辑是否合理
- 增加超时时间配置
- 优化任务性能

## 📊 监控和运维

### 1. 监控指标

**系统指标：**
- 调度器运行状态
- 任务队列长度
- 执行器使用情况
- 数据库连接状态

**业务指标：**
- 任务执行成功率
- 任务平均执行时间
- 失败任务数量
- 告警触发次数

### 2. 监控工具

**Grafana Dashboard 配置：**

```json
{
  "dashboard": {
    "title": "定时任务监控",
    "panels": [
      {
        "title": "任务执行成功率",
        "type": "stat",
        "targets": [
          {
            "expr": "rate(task_success_total[1h])"
          }
        ]
      },
      {
        "title": "任务执行时间",
        "type": "graph",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, task_duration_seconds)"
          }
        ]
      }
    ]
  }
}
```

### 3. 运维手册

#### 日常运维

**每日检查：**
- 检查任务执行状态
- 查看错误日志
- 确认告警通知正常

**每周维护：**
- 清理过期日志
- 检查系统资源使用
- 分析任务性能趋势

**每月优化：**
- 优化慢任务
- 调整调度配置
- 更新监控指标

#### 故障处理

**任务执行失败：**
1. 查看任务执行日志
2. 检查相关系统状态
3. 手动执行任务验证
4. 修复问题并重新调度

**调度器异常：**
1. 检查系统资源
2. 重启调度服务
3. 验证数据库连接
4. 恢复任务调度

## 🎯 性能优化

### 1. 数据库优化

**索引优化：**
```javascript
// 任务执行日志查询优化
db.task_execution_logs.createIndex({
  "task_name": 1,
  "created_at": -1
})

// 任务配置查询优化
db.task_configs.createIndex({
  "enabled": 1,
  "task_name": 1
})
```

**数据分片：**
```javascript
// 按时间分片执行日志
db.task_execution_logs.createIndex({
  "created_at": 1
})

// 配置TTL自动删除过期日志
db.task_execution_logs.createIndex(
  { "created_at": 1 },
  { expireAfterSeconds: 7776000 }  // 90天
)
```

### 2. 任务执行优化

**批量处理：**
```python
# 使用批量操作减少数据库访问
def process_customers_in_batches(customers, batch_size=100):
    for i in range(0, len(customers), batch_size):
        batch = customers[i:i + batch_size]
        yield batch
```

**并发控制：**
```python
# 限制并发任务数量
executor = ThreadPoolExecutor(max_workers=5)

# 使用信号量控制并发
semaphore = asyncio.Semaphore(3)

async def process_with_semaphore(item):
    async with semaphore:
        return await process_item(item)
```

## 📚 文档和维护

### 1. 文档结构

```
docs/
├── pages/
│   └── 通用定时任务/
│       ├── 通用定时任务模块设计方案.md
│       ├── 业务任务实现示例.md
│       ├── 实施指南.md
│       ├── API文档.md
│       ├── 运维手册.md
│       └── 故障排查手册.md
```

### 2. 知识库

**常见问题解答：**
- 如何创建新任务？
- 如何配置任务调度？
- 如何监控任务执行？
- 如何处理任务失败？

**最佳实践：**
- 任务设计原则
- 性能优化技巧
- 错误处理策略
- 监控告警配置

---

## 📋 实施检查清单

### 开发阶段
- [ ] 基础框架完成
- [ ] 监控系统完成
- [ ] API接口完成
- [ ] 业务任务迁移完成
- [ ] 单元测试完成
- [ ] 集成测试完成

### 部署阶段
- [ ] 环境配置完成
- [ ] 数据库初始化完成
- [ ] 应用部署完成
- [ ] 监控配置完成
- [ ] 告警配置完成
- [ ] 文档更新完成

### 验收阶段
- [ ] 功能验证完成
- [ ] 性能测试通过
- [ ] 稳定性测试通过
- [ ] 用户培训完成
- [ ] 运维交接完成

---

*最后更新时间：2025-11-11*
*版本：v1.0*